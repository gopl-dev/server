DS stands for Data Structure. All data models belonging to the app should be stored here.

Global functions should not be created. Instead, use methods on types (when necessary) and private helper functions.